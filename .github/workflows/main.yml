- name: Update go.sum
  run: |
    go mod tidy
    git config user.name 'github-actions[bot]'
    git config user.email 'github-actions[bot]@users.noreply.github.com'
    git add go.sum
    git diff --staged --quiet || git commit -m "chore: update go.sum"
    git push
